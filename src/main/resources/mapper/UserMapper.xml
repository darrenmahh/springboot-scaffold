<?xml version="1.0" encoding="UTF-8" ?>
<!DOCTYPE mapper
        PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
        "https://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="org.example.mapper.UserMapper">
    <insert id="add" parameterType="org.example.entity.User">
        insert into user(username,password,created_time)
        values(#{username},#{password},now())
        <selectKey keyProperty="id" resultType="int" order="AFTER">
            select last_insert_id()
        </selectKey>
    </insert>

    <select id="findUserByUsername" resultType="org.example.entity.User">
        select * from user where username=#{username}
    </select>
    <select id="findUserById" resultType="org.example.entity.User">
        select * from user where id=#{id}
    </select>


</mapper>